from __future__ import annotations

from typing import Any


def main() -> None:
    # 核心业务代码目前只需要一个真实的页面入口。
    # 这里保留独立模块入口，后续可以直接挂接到调度器或外部 Runner。
    raise NotImplementedError("frontend.core.main 尚未实现。")
